To run the code, first extract all of the files from the zip file. Then run "python main.py [test file name]" in the command line where [test file name] is replaced by the file's name which you would like to run.